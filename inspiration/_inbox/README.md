# Inbox

Drop raw images or links here — no writing required. Claude turns them into `entries/`.
If you have a thought about why it caught you, put it in a `.md` or the filename; if not, just the file is fine.
